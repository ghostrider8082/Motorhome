{
  "name": "motorhome",
  "version": "1.0.0",
  "description": "Un portale per la comparazione di motocicli",
  "main": "tailwind.config.js",
  "scripts": {
    "build":"npx tailwindcss -i ./input.css -o ./css/style.css --minify",
    "watch":"npx tailwindcss -i ./input.css -o ./css/style.css --watch --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
